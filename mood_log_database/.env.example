# MoodLog Database - Environment Example

# Core PostgreSQL connection parameters
POSTGRES_HOST=localhost
POSTGRES_PORT=5000
POSTGRES_DB=myapp
POSTGRES_USER=appuser
POSTGRES_PASSWORD=dbuser123

# Unified connection URL used by apps/tools
# Example: postgresql://USER:PASSWORD@HOST:PORT/DB
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Seed control: set to "true" to run seed data after migrations
SEED=true
